manifest_version: 1

version:
  name: v1
  label: "1.0.0"
  comment: "Airbrx Cost Intelligence v1"

artifacts:
  setup_script: setup.sql
  default_streamlit: src/streamlit_app.py
  extension_code: true

privileges:
  - IMPORTED PRIVILEGES ON SNOWFLAKE DB:
      description: "Read ACCOUNT_USAGE query history and cost attribution views"
  - EXECUTE TASK:
      description: "Run the scheduled 2-hour analysis task"
